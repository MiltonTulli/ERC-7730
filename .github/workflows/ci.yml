name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Biome
        uses: biomejs/setup-biome@v2
        with:
          version: 1.9.0

      - name: Run Biome
        run: biome ci .

  build-and-test:
    name: Build & Test (Node ${{ matrix.node }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [20, 22]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all packages
        run: pnpm build

      - name: Type check (after build)
        run: pnpm typecheck

      - name: Run tests
        run: pnpm test

      - name: Check package exports
        run: |
          cd packages/sdk
          node -e "import('@erc7730/sdk').then(m => console.log('SDK exports:', Object.keys(m)))"

      - name: publint + attw
        working-directory: packages/sdk
        run: |
          pnpm dlx publint
          # SDK is ESM-only (type: module, export conditions are import/types).
          # attw 0.18 has no optional-peer-dependency rule; esm-only skips node10/CJS.
          pnpm dlx @arethetypeswrong/cli --pack . --profile esm-only

  changeset:
    name: Changeset (when SDK source changes)
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Require a changeset for SDK source or package.json changes
        run: |
          git fetch origin main --depth=1
          CHANGED=$(git diff --name-only origin/main...HEAD)
          echo "Changed files:"
          echo "$CHANGED"
          SDK_TOUCHED=$(echo "$CHANGED" | grep -E '^(packages/sdk/src/|packages/sdk/package.json$)' || true)
          CHANGESETS=$(echo "$CHANGED" | grep -E '^\.changeset/.+\.md$' | grep -v 'README.md' || true)
          if [ -n "$SDK_TOUCHED" ] && [ -z "$CHANGESETS" ]; then
            echo "::error::SDK source or package.json changed without a .changeset/*.md file. Run: pnpm changeset"
            exit 1
          fi
          echo "Changeset check passed."
