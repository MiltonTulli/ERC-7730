---
"@erc7730/sdk": minor
---

Add pluggable TrustPolicy (`officialOnlyPolicy`, `officialOrLocalPolicy`, `composePolicies`) so wallets decide which descriptors to accept. Registry `extend()` hits are tagged `local-override`.
