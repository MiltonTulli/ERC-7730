---
"@erc7730/cli": minor
---

Initial `@erc7730/cli`: `erc7730 generate` / `lint` / `preview` / `diff` plus `registry update`. Generate writes v2-valid drafts; lint exits 1 on errors; preview prints intent + fields.
